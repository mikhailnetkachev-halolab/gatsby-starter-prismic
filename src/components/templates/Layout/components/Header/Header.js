import React from 'react';
import cs from 'classnames';

import styles from './styles.module.scss';

const Header = () => {
  return <div className={cs(styles.container, 'container')}>Header</div>;
};

export default Header;
